# Portfolio card covers

포트폴리오 카드 배경 이미지를 이 폴더에 넣고 `script.js`의 해당 `portfolioItems` 항목에서
`cover` 값을 상대 경로로 지정한다.

```js
cover: 'assets/images/portfolio/revive-system-cover.webp',
```

이미지는 카드 전체를 채우는 `cover` 방식으로 표시되며, 텍스트 가독성을 위한 어두운 오버레이가 자동 적용된다.
